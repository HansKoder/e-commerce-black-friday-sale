meta {
  name: add-cart-item
  type: http
  seq: 1
}

post {
  url: {{baseURL}}{{cartService}}/items/save
  body: json
  auth: none
}

body:json {
  {
    "customerId": "{{customerId}}",
    "productId": "{{productId2}}",
    "cant": 2,
    "price": 800
  }
}
