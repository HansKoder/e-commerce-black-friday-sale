meta {
  name: add-cart-item
  type: http
  seq: 1
}

post {
  url: {{baseURL}}{{cartService}}/items/save
  body: json
  auth: none
}

body:json {
  {
    "customerId": "{{customerId}}",
    "productId": "{{productId}}",
    "cant": 1,
    "price": 1000
  }
}
