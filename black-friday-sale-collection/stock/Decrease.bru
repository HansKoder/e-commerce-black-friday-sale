meta {
  name: Decrease
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/v1/stock/update
  body: json
  auth: inherit
}

body:json {
  {
    "productId": "{{productId}}",
    "value": 2,
    "operation": "DECREASE"
  }
}
